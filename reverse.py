
def reverse(): 
    original = "here i am"
    new_text = ""

    length = len(original) - 1
    for i in range(length + 1):
        return(length - i)
    new_text = new_text + original[length - i]

    return new_text